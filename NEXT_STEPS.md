# Next Steps

1. Add equivalent smoke-gate scripts for `resume-basic`, `comic-basic`, and `storyboard-basic`.
2. Add a combined `examples:smoke` aggregator command that runs all example smoke gates in CI.
3. Emit optional JSON summaries from smoke scripts for machine-readable CI annotations.
